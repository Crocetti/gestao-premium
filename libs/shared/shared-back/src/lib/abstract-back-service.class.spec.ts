import { AbstractBackService } from './abstract-back-service.class';

describe('AbstractBackService', () => {
  it('should create an instance', () => {
    expect(new AbstractBackService()).toBeTruthy();
  });
});
