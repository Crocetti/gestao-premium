server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
    }

    location /api {
        proxy_pass http://google.com/;
        proxy_http_version 1.1;
        index index.html index.htm;
    }
}
